name: flutter_print_khmer_language
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  #Services
  blue_thermal_printer: ^1.2.0
  flutter_blue: ^0.8.0

  #Utils
  shared_preferences: ^2.0.12

  #Widgets
  image: ^3.1.3

  #Message
  fluttertoast: ^8.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
